# 未实现功能清单

本文档总结了已设计但尚未实现的功能，用于后续开发参考。

## 2025-12-17 进度对齐（新增）

本节用于将 TODO 与实际开发进度对齐，并按“接下来四大主任务”重排优先级：
- **完善战斗引擎**
- **优化战斗界面与动画显示**
- **开发模组设计与卡牌生成功能（允许添加卡牌）**
- **更新敌方 AI 的部署逻辑**

### 已落地的关键能力（用于后续开发的基础设施）
- [x] **战斗快照（Snapshot）**：导出/恢复 `BattleState`，快照中清理计算字段，运行时可重新派生。
- [x] **战斗日志（Log）**：引擎回合内通过 `pendingLogEntries` 累积，UI 统一提交到 `BattleLog`。
- [x] **回合增量日志保存**：每回合把新增日志以下载方式保存（浏览器环境限制下的实现）。
- [x] **调试控制台扩展**：提供快照/日志页签与导出按钮，提升问题复现效率。
- [x] **基地多目标反击（可配置）**：新增 `Base.maxTargets`，当前示例配置为 2。

### 接下来（推荐优先级）
1. **完善战斗引擎**：规则正确性与可解释性（日志可复现）优先于新增内容。
2. **界面与动画**：以“信息呈现清晰 + 状态变化可视化”为目标逐步增强。
3. **模组与卡牌生成**：建立可扩展的数据驱动入口（JSON/脚本）与编辑流程。
4. **敌方 AI 部署**：先做到“规则一致 + 可调参数”，再做“策略性”。

## 已实现功能 ✅

### 核心战斗系统
- [x] 基础战斗引擎
- [x] 单位部署系统
- [x] 单位移动系统
- [x] 单位攻击系统
- [x] 基地系统
- [x] 胜利/失败条件检查
- [x] 回合制流程

### 部署系统
- [x] 部署延迟机制
- [x] 部署信标系统
- [x] 反部署机制
- [x] 反部署延迟
- [x] 基地部署（跳过延迟）

### 卡牌系统
- [x] 战斗副本系统（BattleCard）
- [x] 卡牌损耗系统（HP上限降低）
- [x] 卡牌状态管理（available/killed）
- [x] 简单命名系统（基础名称 + 序号）
- [x] 阵亡单位管理

### 资源系统
- [x] 命令点数系统
- [x] 命令点数恢复
- [x] 手牌系统
- [x] 补牌系统
- [x] 手牌上限

### UI系统
- [x] 战场网格显示
- [x] 单位显示
- [x] 基地HP显示
- [x] 手牌显示
- [x] 单位HP显示
- [x] 部署状态显示
- [x] 调试控制台：快照/日志查看与导出

## 未实现功能 ❌

### 1. 更复杂的攻击规则

#### 设计状态
- ✅ 已设计优先级系统
- ✅ 已设计不同射程
- ❌ 未实现代码

#### 需要实现
- [ ] 索敌优先级系统扩展（当前已支持 per-unit `targetPriority`，实现 prefer_units / prefer_buildings / buildings_only；后续扩展血量/类型/对空优先等）
- [ ] 不同射程的攻击范围检查（目前已通过 attackPatternConfig 支持格子化射程；需要统一 range 与格子模板的显示/编辑体验）
- [ ] 目标选择算法增强（低血量优先、精英优先、特定标签优先）
- [ ] 溅射伤害系统增强（当前已支持基于格子的 SplashPatternCell；后续支持多形状编辑工具/与对空配置联动）

#### 相关文件
- `types.ts`: 需要扩展 `UnitTemplate` 接口
- `engine.ts`: 需要重写 `performAttacks` 函数

---

### 2. 特殊移动模式

#### 设计状态
- ✅ 已设计标准前进模式
- ✅ 已预留扩展接口
- ❌ 未实现特殊模式

#### 需要实现
- [ ] 斜向移动模式（当前仅通过引流地形实现“纵向一格”，尚未支持真正的对角线移动）
- [ ] 跳跃/传送模式
- [ ] 飞行单位（飞越障碍，忽略 blocksGround/blocksFull 等）
- [ ] 自定义移动模式支持（为特定单位提供脚本化移动规则）

#### 相关文件
- `types.ts`: 需要扩展 `MovePattern` 类型
- `engine.ts`: 需要扩展 `moveUnitsForward` 函数

---

### 3. 卡牌组织标识系统

#### 设计状态
- ✅ 已设计完整数据结构
- ✅ 已定义命名规则
- ✅ 已预留扩展接口
- ❌ 未实现代码

#### 需要实现
- [ ] `OrganizationIdentifier` 接口实现
- [ ] 组织生成器（`OrganizationGenerator`）
- [ ] 不同兵种的命名模板
- [ ] 显示名称自动生成
- [ ] 组织层级的阵亡管理

#### 相关文件
- `types.ts`: 需要添加组织标识接口
- `engine.ts`: 需要实现组织生成逻辑
- `inventory.ts`: 未来背包系统中的组织管理

---

### 4. 战斗事件系统

#### 设计状态
- ✅ 已定义 `BattleEvent` 接口
- ✅ 已定义事件类型
- ❌ 未实现事件处理逻辑

#### 需要实现
- [ ] 事件触发器系统
- [ ] 事件效果系统（中立单位、环境伤害、支援）
- [ ] 事件UI显示
- [ ] 事件配置系统

#### 相关文件
- `types.ts`: `BattleEvent` 接口已定义
- `engine.ts`: 需要实现事件处理逻辑
- `App.tsx`: 需要添加事件UI显示

---

### 5. 战斗结算增强

#### 设计状态
- ✅ 已实现基础结算（HP损耗、阵亡状态）
- ❌ 未实现经验值和掉落

#### 需要实现
- [ ] 经验值系统
- [ ] 单位升级系统
- [ ] 掉落物品系统
- [ ] 结算界面显示

#### 相关文件
- `types.ts`: 需要扩展 `BattleResult` 接口
- `engine.ts`: 需要扩展 `calculateBattleResult` 函数
- `App.tsx`: 需要添加结算界面

---

### 6. 战斗回放系统

#### 设计状态
- ⚠️ 部分实现（基础设施已具备）

#### 需要实现
- [x] 状态快照系统（导出/恢复 BattleState）
- [x] 回合增量日志（用于复盘与诊断）
- [ ] 操作记录系统（记录玩家输入：部署/反部署/结束回合等，确保“输入可重放”）
- [ ] 回放播放器（按“输入 + 引擎确定性”驱动重演；或按“快照序列”播放）
- [ ] 快进/快退功能（基于快照索引/关键帧）

---

### 7. 多人对战支持

#### 设计状态
- ❌ 未设计
- ❌ 未实现

#### 需要实现
- [ ] 网络协议设计
- [ ] 服务器架构
- [ ] 客户端同步逻辑
- [ ] 断线重连机制

---

### 8. UI/UX 改进

#### 需要实现
- [ ] 更美观的卡牌UI
- [ ] 单位动画效果
- [ ] 攻击动画
- [ ] 伤害数字显示
- [ ] 音效系统
- [ ] 背景音乐

---

## 主线任务拆分（2025-12-17）

### A. 完善战斗引擎（高优先级）
- [ ] 引擎确定性：为涉及随机的逻辑引入可注入 RNG seed，并将结果写入日志（便于复现）。
- [ ] 规则统一：把“普通攻击/基地反击/技能攻击”统一到同一套技能/攻击执行框架（减少分叉逻辑）。
- [ ] 阻挡/占格规则文档化：明确 `space(ground/air/full)` 与 `terrain blocks*` 的最终规则矩阵，并在索敌/移动/部署三处一致落地。
- [ ] 伤害管线单元测试：对暴击/技能倍率/地形受伤倍率/溅射建立可复现测试用例（与日志字段对齐）。
- [ ] 更复杂的目标选择：扩展优先级规则（低血量/标签/对空优先等），并将候选集与排序过程写入日志。

### B. 优化战斗界面与动画显示（中高优先级）
- [ ] 空地同格展示完善：在同格存在空地单位时，稳定显示规则 + 明确“+N”叠加提示与 hover 列表。
- [ ] 动画基线：移动/攻击/受伤/死亡 的最小动画（不追求华丽，追求信息明确）。
- [ ] 伤害数字与事件提示：基于日志/事件流的浮动数字与战报条。
- [ ] 性能：大回合日志/单位数量增加时的渲染优化（memo/虚拟列表）。

### C. 模组设计与卡牌生成（中优先级）
- [ ] 模组数据格式：定义 `mod.json`（单位模板、卡牌池、地形、AI 参数、战役入口）。
- [ ] 卡牌生成器：从“模板 + 权重 + 稀有度规则”生成 `CardInventory/BattleCard`（含命名、初始数值、可选浮动）。
- [ ] 模组加载流程：启动时加载/合并多个模组，处理 ID 冲突与版本号。
- [ ] 编辑入口（先简化）：提供导入/导出 JSON 的 UI（后续再做可视化编辑器）。

### D. 敌方 AI 部署逻辑（中优先级）
- [ ] AI 目标：先实现“合法且稳定”的部署（不违规、不堵死自己），再实现策略（克制/节奏/资源管理）。
- [ ] AI 部署规则：基于战线压力、距离基地、对空需求、当前命令点与手牌，输出部署决策。
- [ ] AI 可调参：把策略权重/阈值配置化（写入模组或战役配置）。
- [ ] AI 决策日志：每回合记录 AI 候选卡牌、评分、最终选择与原因（必须可诊断）。

---

### 9. 性能优化

#### 需要实现
- [ ] 状态更新优化（减少不必要的重渲染）
- [ ] 大型战场优化（虚拟滚动）
- [ ] 内存管理优化
- [ ] 战斗计算优化

---

### 10. 战役 / 星系 结构与战术Roguelite循环

#### 设计状态
- ✅ 已初步确定宏观结构：**星系 → 章节 → 战役**。
- ✅ 已在 `campaignConfig.ts` 中实现最小可用结构：`初始星系-教程章节-战役 0.1 Test`，作为战斗入口。
- ❌ 未实现星系内资源重置 / 遗落战利品 / 科技与指挥官成长等系统。

#### 目标结构（设计草案）
- **星系（Galaxy）**
  - 决定本星系中可能遭遇的种族、事件与战役节点。
  - 进入新星系时：
    - 重置：仓库、背包、可用军队列表（相当于重新开一局）。
    - 保留：科技加点、指挥官等级、永久解锁的亲卫位/保险库位等“账号级/指挥官级”进度。
- **章节（GalaxyChapter）**
  - 星系内的一个连续行动段落（类似战役章节）。
  - 每个章节开始时：
    - 获得一次性补给（资源、基础兵员/卡牌）。
    - 整理仓库与背包、编制军队编制；之后进入该章节下的战役链路。
  - 同一章节内的多场战役之间：
    - **不能**从星系“全局仓库”再取物资，只能使用章节开始时发放/整理好的内容和战役中获取的临时补给。
- **战役（GalaxyBattle）**
  - 单场战斗（对应一个 `BattleConfig`），消耗和掉落都只在章节局部范围内生效。
  - 失败处理逻辑（Roguelite 要素）：
    - 除了科技加点带来的“亲卫”与“保险库插槽”内的物品，其余背包物品与军队全部判定为**遗落在失败战役的地图上**。
    - 再次行军经过该战役节点时：
      - 触发“打捞/找回事件”：根据配置只返还部分遗落军队/物资（可能带惩罚或事件分支）。

#### 需要实现
- [ ] `Galaxy / GalaxyChapter / GalaxyBattle` 与战役进度保存结构的最终版（持久化到战役存档）。
- [ ] 星系级重置逻辑：进入新星系时，重置仓库/背包/军队，只保留科技与指挥官进度。
- [ ] 章节级开局流程：补给发放、背包整理、可用军队构建、进入第一场战役。
- [ ] 章节内战役失败逻辑：
  - [ ] 识别科技/保险库保护的单位与物品，防止被清空。
  - [ ] 将其余背包与在场军队标记为“遗落在某战役节点”。
  - [ ] 为每个遗落节点记录可回收的军队/物资信息。
- [ ] 再次抵达遗落节点时的“打捞”事件流程：
  - [ ] 计算可回收比例/条目（可能受科技/难度影响）。
  - [ ] 允许玩家在有限的回收名额中做选择（优先回收精锐、卡组核心等）。
- [ ] 指挥官层面的科技加点与亲卫/保险库位系统接口（为后续 UI 实现预留字段）。

#### 相关文件
- `campaignConfig.ts`: 当前已包含 `Galaxy / GalaxyChapter / GalaxyBattle` MVP 结构，需要进一步扩展字段与持久化。
- `App.tsx`: 主菜单/章节/战役选择 UI，需要后续接入资源/失败/打捞等流程。
- `inventory/*`: 未来会拆分星系仓库、章节背包、战役内临时资源等模块。

---

## 开发优先级建议

### 第一阶段（核心功能完善）
1. 更复杂的攻击规则（基于 attackPatternConfig 和锁定目标系统继续扩展）
2. UI/UX 改进
3. 地形高级效果（目前已实现基础 directional.funnelDirection，引流逻辑仍为 MVP）

### 第二阶段（功能增强）
1. 卡牌组织标识系统
2. 特殊移动模式
3. 战斗事件系统

### 第三阶段（高级功能）
1. 战斗结算增强
2. 战斗回放系统
3. 多人对战支持

---

## 已实现（新增概述）
- 士气系统：全局士气0~120，影响攻/暴击；部署/击杀/反部署/基地受击变化，UI显示。
- 部署信标与延迟：占格信标、延迟后转本体，本回合不动不攻。
- 基地伤害与士气：基地受击按伤害扣士气（系数0.5，最少1）。
- 地形基础版：moveCost/moveBonus/攻受伤系数、阻挡，示例道路/掩体/困难地形；移动支持逐级退让避免卡死；攻击/基地伤害套用地形修正。

---

## 注意事项

- 所有新功能都应该保持向后兼容
- 实现前应该先更新设计文档
- 代码应该遵循现有的代码风格和架构
- 应该添加适当的测试用例

